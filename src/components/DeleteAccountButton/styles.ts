import {StyleSheet} from 'react-native';

const SEIZE = 100;

const styles = StyleSheet.create({
  button: {
    position: 'absolute',
    alignSelf: 'center',
    height: SEIZE,
    width: SEIZE,
    borderRadius: SEIZE / 2,
  },
});

export default styles;
