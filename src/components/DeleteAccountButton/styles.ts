import {StyleSheet} from 'react-native';

const SIZE = 100;

const styles = StyleSheet.create({
  button: {
    position: 'absolute',
    alignSelf: 'center',
    height: SIZE,
    width: SIZE,
    borderRadius: SIZE / 2,
  },
});

export default styles;
